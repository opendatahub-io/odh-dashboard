apiVersion: v1
kind: Service
metadata:
  name: rhaii-dashboard
  labels:
    app: rhaii-dashboard
spec:
  type: ClusterIP
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
  selector:
    app: rhaii-dashboard
