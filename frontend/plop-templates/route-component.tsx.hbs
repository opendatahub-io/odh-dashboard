import React from 'react';
import { Routes, Route } from 'react-router-dom';
import {{{pascalCase componentName}}}Page from './{{{pascalCase componentName}}}Page';

const {{{pascalCase componentName}}}: React.FC = () => (
  <Routes>
    <Route path="/" element={<{{{pascalCase componentName}}}Page />} />
  </Routes>
);

export default {{{pascalCase componentName}}}; 