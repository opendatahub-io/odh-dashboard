export enum PipelineRunType {
  Active = 'active',
  Archived = 'archived',
  Scheduled = 'scheduled',
}

export enum PipelineRunTabTitle {
  Active = 'Active runs',
  Archived = 'Archived runs',
  Schedules = 'Schedules',
}
