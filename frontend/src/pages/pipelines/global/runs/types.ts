export enum PipelineRunType {
  Active = 'active',
  Archived = 'archived',
  Scheduled = 'scheduled',
}

export enum PipelineRunTabTitle {
  Active = 'Active',
  Archived = 'Archived',
  Schedules = 'Schedules',
}
