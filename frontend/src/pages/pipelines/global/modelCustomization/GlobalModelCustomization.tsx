import * as React from 'react';
import ModelCustomization from '~/pages/pipelines/global/modelCustomization/ModelCustomization';

const GlobalModelCustomization: React.FC = () => <ModelCustomization />;

export default GlobalModelCustomization;
