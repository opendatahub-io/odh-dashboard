name: E2E Direct Commit PR Tests

on: [push, pull_request]

permissions:
  contents: read

env:
  NODE_VERSION: 20.x
  DO_NOT_TRACK: 1

jobs:
  get-test-tags:
    runs-on: self-hosted
    outputs:
      test-tags: ${{ steps.set-tags.outputs.test-tags }}
    steps:
      - name: Set default test tags
        id: set-tags
        # Created 2 new tags - @ci-dashboard-set-1 and @ci-dashboard-set-2
        # ToDO: Use turbo to run tests according to PR changes.
        run: |
          echo "test-tags=[\"@ci-dashboard-set-1\",\"@ci-dashboard-set-2\"]" >> $GITHUB_OUTPUT

  e2e-tests:
    needs: [get-test-tags]
    runs-on: self-hosted
    strategy:
      fail-fast: false
      matrix:
        test-tag: ${{ fromJson(needs.get-test-tags.outputs.test-tags) }}
    steps:
      - name: Calculate unique port for this workflow run
        run: |
          # Dynamic port per PR/run to support parallel execution across different PRs
          # Port range: 4000-4999 (1000 ports available)
          BASE_PORT=$((4000 + (${{ github.run_id }} % 100) * 10))
          
          # Add matrix offset to separate concurrent jobs within same PR
          if [[ "${{ matrix.test-tag }}" == *"set-1"* ]]; then
            MATRIX_OFFSET=0
          elif [[ "${{ matrix.test-tag }}" == *"set-2"* ]]; then
            MATRIX_OFFSET=1
          else
            MATRIX_OFFSET=2
          fi
          
          CYPRESS_PORT=$((BASE_PORT + MATRIX_OFFSET))
          echo "CYPRESS_PORT=$CYPRESS_PORT" >> $GITHUB_ENV
          
          echo "📍 Run ${{ github.run_id }} / Tag ${{ matrix.test-tag }}"
          echo "   BASE_PORT: $BASE_PORT (from run_id % 100 * 10)"
          echo "   MATRIX_OFFSET: $MATRIX_OFFSET"
          echo "   FINAL PORT: $CYPRESS_PORT"

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache npm dependencies
        uses: actions/cache@v4
        id: npm-cache
        with:
          path: |
            ~/.cache/Cypress
            **/node_modules
          key: ${{ runner.os }}-${{ env.NODE_VERSION }}-all-modules-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-${{ env.NODE_VERSION }}-all-modules-

      - name: Setup Node.js ${{ env.NODE_VERSION }}
        if: steps.npm-cache.outputs.cache-hit != 'true'
        uses: actions/setup-node@v4.3.0
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install dependencies
        if: steps.npm-cache.outputs.cache-hit != 'true'
        run: npm ci

      - name: Cache turbo build artifacts
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/.turbo
          key: ${{ runner.os }}-${{ env.NODE_VERSION }}-turbo-${{ github.sha }}-e2e
          restore-keys: |
            ${{ runner.os }}-${{ env.NODE_VERSION }}-turbo-

      - name: Cache OpenShift CLI tarball
        uses: actions/cache@v4
        id: oc-cache
        with:
          path: ${{ runner.temp }}/oc.tar.gz
          key: ${{ runner.os }}-oc-tarball-${{ env.OC_VERSION || '4.15.0' }}

      - name: Check and Install OpenShift CLI
        run: |
          echo "🔍 Checking if OpenShift CLI (oc) is installed..."

          # Check if oc is already installed
          if command -v oc &> /dev/null; then
            echo "✅ OpenShift CLI (oc) is already installed"
            oc version --client
          else
            echo "❌ OpenShift CLI (oc) not found, installing..."
            echo "📦 Installing oc CLI for Linux..."

            OC_TARBALL="${{ runner.temp }}/oc.tar.gz"

            # Check if tarball was cached
            if [ -f "$OC_TARBALL" ]; then
              echo "✅ Using cached OpenShift CLI tarball"
            else
              echo "📥 Downloading OpenShift CLI tarball..."
              curl -LO https://mirror.openshift.com/pub/openshift-v4/clients/oc/latest/linux/oc.tar.gz -o "$OC_TARBALL"
            fi

            # Extract and install oc
            echo "📦 Extracting OpenShift CLI..."
            tar -xzf "$OC_TARBALL"
            sudo mv oc /usr/local/bin/
            sudo chmod +x /usr/local/bin/oc

            # Verify installation
            echo "✅ OpenShift CLI installed successfully"
            oc version --client
          fi

          # Final verification
          echo "🎯 Final oc version check:"
          oc version --client || echo "⚠️  Could not determine oc version"

      - name: Install Cypress Dependencies
        run: |
          echo "🔧 Installing Cypress system dependencies for Fedora (based on Cypress docs)..."

          # Use the simplified approach from Cypress documentation for Amazon Linux 2023 (Fedora-compatible)
          if [[ "$RUNNER_OS" == "Linux" ]]; then
            echo "📦 Installing minimal Cypress dependencies for Linux..."

            # Based on Cypress docs for Amazon Linux 2023 - Fedora compatible
            echo "Installing core dependencies..."
            sudo dnf install -y xorg-x11-server-Xvfb gtk3-devel nss alsa-lib

            # Add the missing ATK packages that Cypress actually needs
            echo "Installing ATK dependencies..."
            sudo dnf install -y atk atk-devel at-spi2-atk

            # Add additional GUI libraries that might be needed
            echo "Installing additional GUI libraries..."
            sudo dnf install -y libgbm libgbm-devel libnotify libnotify-devel

            # Update library cache
            sudo ldconfig

            # Verify installations
            echo "🔍 Verifying installations..."
            if command -v Xvfb &> /dev/null; then
              echo "✅ Xvfb installed"
            else
              echo "❌ Xvfb missing"
            fi

            if ldconfig -p | grep -q libnss3; then
              echo "✅ NSS libraries installed"
            else
              echo "❌ NSS libraries missing"
            fi

            if ldconfig -p | grep -q libatk; then
              echo "✅ ATK libraries installed (fixes libatk-1.0.so.0 error)"
              echo "📋 ATK libraries found:"
              ldconfig -p | grep libatk
            else
              echo "❌ ATK libraries missing - checking what we have..."
              sudo dnf list installed | grep -i atk || echo "No ATK packages found"
              find /usr/lib* -name "*libatk*" 2>/dev/null || echo "No ATK libraries found in filesystem"
            fi

            echo "🎯 Cypress dependencies installation completed"
          fi

      - name: Debug available secrets
        run: |
          echo "🔍 DEBUG: Checking available secrets..."
          echo "Event name: ${{ github.event_name }}"
          echo "Repository: ${{ github.repository }}"
          echo "Repository owner: ${{ github.repository_owner }}"
          echo "Ref: ${{ github.ref }}"
          echo "Base ref: ${{ github.base_ref }}"
          echo "Head ref: ${{ github.head_ref }}"

          # Check for specific secrets without revealing values
          if [ -n "${{ secrets.GITLAB_TOKEN }}" ]; then
            echo "✅ GITLAB_TOKEN: Available (length: ${#GITLAB_TOKEN})"
          else
            echo "❌ GITLAB_TOKEN: Not available or empty"
          fi

          if [ -n "${{ secrets.GITLAB_TEST_VARS_URL }}" ]; then
            echo "✅ GITLAB_TEST_VARS_URL: Available (length: ${#GITLAB_TEST_VARS_URL})"
          else
            echo "❌ GITLAB_TEST_VARS_URL: Not available or empty"
          fi

          # Check other potentially relevant secrets
          if [ -n "${{ secrets.OC_SERVER }}" ]; then
            echo "✅ OC_SERVER: Available"
          else
            echo "❌ OC_SERVER: Not available"
          fi

          if [ -n "${{ secrets.OCP_CONSOLE_URL }}" ]; then
            echo "✅ OCP_CONSOLE_URL: Available"
          else
            echo "❌ OCP_CONSOLE_URL: Not available"
          fi

          if [ -n "${{ secrets.ODH_DASHBOARD_URL }}" ]; then
            echo "✅ ODH_DASHBOARD_URL: Available"
          else
            echo "❌ ODH_DASHBOARD_URL: Not available"
          fi

          # List all available secrets (names only)
          echo "📋 Available secrets in this environment:"
          env | grep -E '^[^=]+=' | sed 's/=.*//' | grep -i secret || echo "No SECRET variables found"

      - name: Download test configuration
        run: |
          # Debug: Show what we're working with
          echo "🔧 DEBUG: Attempting to download test configuration..."
          echo "GITLAB_TOKEN present: $([ -n "${{ secrets.GITLAB_TOKEN }}" ] && echo 'YES' || echo 'NO')"
          echo "GITLAB_TEST_VARS_URL present: $([ -n "${{ secrets.GITLAB_TEST_VARS_URL }}" ] && echo 'YES' || echo 'NO')"

          # Try to download test-variables.yml from GitLab securely
          if [ -n "${{ secrets.GITLAB_TOKEN }}" ] && [ -n "${{ secrets.GITLAB_TEST_VARS_URL }}" ]; then
            echo "🔄 Both secrets are available, attempting download..."
            echo "Token length: ${#GITLAB_TOKEN}"
            echo "URL length: ${#GITLAB_TEST_VARS_URL}"

            # Try with SSL verification disabled (for self-signed certs)
            if curl -f -k -H "Authorization: Bearer ${{ secrets.GITLAB_TOKEN }}" \
                        "${{ secrets.GITLAB_TEST_VARS_URL }}" \
                        -o ${{ github.workspace }}/frontend/src/__tests__/cypress/test-variables.yml; then
              echo "✅ Successfully downloaded test configuration from GitLab"
              ls -la ${{ github.workspace }}/frontend/src/__tests__/cypress/test-variables.yml
            else
              echo "❌ Failed to download from GitLab"
              echo "Curl exit code: $?"

              # Try to get more details about the failure
              echo "Trying without -f to see error details..."
              curl -k -H "Authorization: Bearer ${{ secrets.GITLAB_TOKEN }}" \
                  "${{ secrets.GITLAB_TEST_VARS_URL }}" \
                  -o /tmp/test-response 2>&1 || echo "Curl failed with: $?"

              if [ -f /tmp/test-response ]; then
                echo "GitLab API Response:"
                head -10 /tmp/test-response
              fi

              exit 1
            fi
          else
            echo "⚠️  GitLab secrets not available - cannot download test configuration"
            echo "💡 This workflow requires GITLAB_TOKEN and GITLAB_TEST_VARS_URL secrets to be configured"
            echo "💡 For forked PRs, these secrets need to be available in the upstream repository"
            echo ""
            echo "🔍 DEBUG INFO:"
            echo "- Repository: ${{ github.repository }}"
            echo "- Event: ${{ github.event_name }}"
            echo "- Is fork: $([ '${{ github.repository_owner }}' != 'opendatahub-io' ] && echo 'YES' || echo 'NO')"
            echo "- Base repo: ${{ github.base_ref && github.repository || 'N/A' }}"
            echo ""
            echo "💡 POSSIBLE SOLUTIONS:"
            echo "1. Ensure secrets are configured in upstream repo (opendatahub-io/odh-dashboard)"
            echo "2. For testing, secrets need to be available when PR is merged"
            echo "3. Check secret names match exactly: GITLAB_TOKEN, GITLAB_TEST_VARS_URL"
            exit 1
          fi

      - name: Login to OpenShift cluster
        run: |
          # For forked PRs, skip OpenShift login since secrets are not available
          if [ -z "${{ secrets.OC_SERVER }}" ]; then
            echo "⚠️  OpenShift secrets not available (forked PR) - skipping cluster login"
            echo "💡 Tests will run in mock mode or against local test environment"
            exit 0
          fi

          # Read credentials from downloaded test-variables.yml
          TEST_VARS_FILE="${{ github.workspace }}/frontend/src/__tests__/cypress/test-variables.yml"

          # Extract OC_USERNAME from OCP_ADMIN_USER section
          OC_USERNAME=$(grep -A 10 "^OCP_ADMIN_USER:" "$TEST_VARS_FILE" | grep "USERNAME:" | head -1 | sed 's/.*USERNAME: //' | tr -d ' ')

          # Extract OC_PASSWORD from OCP_ADMIN_USER section
          OC_PASSWORD=$(grep -A 10 "^OCP_ADMIN_USER:" "$TEST_VARS_FILE" | grep "PASSWORD:" | head -1 | sed 's/.*PASSWORD: //' | tr -d ' ')

          # Mask sensitive data in logs
          echo "::add-mask::$OC_PASSWORD"
          echo "::add-mask::$OC_USERNAME"

          # Extract TEST_USER_3_AUTH_TYPE from TEST_USER_3 section
          TEST_USER_3_AUTH_TYPE=$(grep -A 10 "^TEST_USER_3:" "$TEST_VARS_FILE" | grep "AUTH_TYPE:" | head -1 | sed 's/.*AUTH_TYPE: //' | tr -d ' ')

          # Extract TEST_USER_3_USERNAME from TEST_USER_3 section
          TEST_USER_3_USERNAME=$(grep -A 10 "^TEST_USER_3:" "$TEST_VARS_FILE" | grep "USERNAME:" | head -1 | sed 's/.*USERNAME: //' | tr -d ' ')

          # Extract TEST_USER_3_PASSWORD from TEST_USER_3 section
          TEST_USER_3_PASSWORD=$(grep -A 10 "^TEST_USER_3:" "$TEST_VARS_FILE" | grep "PASSWORD:" | head -1 | sed 's/.*PASSWORD: //' | tr -d ' ')

          # Mask TEST_USER_3 credentials in logs (must happen after extraction)
          echo "::add-mask::$TEST_USER_3_USERNAME"
          echo "::add-mask::$TEST_USER_3_PASSWORD"

          # Export TEST_USER_3 credentials as environment variables for Cypress
          echo "TEST_USER_3_AUTH_TYPE=$TEST_USER_3_AUTH_TYPE" >> $GITHUB_ENV
          echo "TEST_USER_3_USERNAME=$TEST_USER_3_USERNAME" >> $GITHUB_ENV
          echo "TEST_USER_3_PASSWORD=$TEST_USER_3_PASSWORD" >> $GITHUB_ENV
          
          echo "Logging in to OpenShift cluster..."
          oc login -u "$OC_USERNAME" -p "$OC_PASSWORD" --server="${{ secrets.OC_SERVER }}" --insecure-skip-tls-verify

          # Export OpenShift configuration for Cypress tests
          echo "🔧 Setting up OpenShift configuration for Cypress tests..."
          export KUBECONFIG="$HOME/.kube/config"
          echo "KUBECONFIG=$KUBECONFIG" >> $GITHUB_ENV

      - name: Set test configuration
        run: |
          # IMPORTANT: Always use localhost with dynamic port for Cypress tests
          # The webpack dev server proxies API calls to the cluster
          echo "🔧 Updating test-variables.yml to use localhost:${CYPRESS_PORT}..."
          sed -i.bak "s|ODH_DASHBOARD_URL:.*|ODH_DASHBOARD_URL: http://localhost:${CYPRESS_PORT}|g" ${{ github.workspace }}/frontend/src/__tests__/cypress/test-variables.yml
          sed -i.bak "s|OCP_CONSOLE_URL:.*|OCP_CONSOLE_URL: http://localhost:${CYPRESS_PORT}|g" ${{ github.workspace }}/frontend/src/__tests__/cypress/test-variables.yml

          echo "📝 Verifying updated URLs:"
          grep "ODH_DASHBOARD_URL:" ${{ github.workspace }}/frontend/src/__tests__/cypress/test-variables.yml | head -3
          grep "OCP_CONSOLE_URL:" ${{ github.workspace }}/frontend/src/__tests__/cypress/test-variables.yml | head -3

          export CY_TEST_CONFIG="${{ github.workspace }}/frontend/src/__tests__/cypress/test-variables.yml"
          echo "✅ Test config set to use localhost:${CYPRESS_PORT}"

      - name: Start Cypress Server
        run: |
          echo "🧹 Targeted cleanup of port ${CYPRESS_PORT} only (preserving other parallel jobs)..."
          
          # ONLY kill processes using OUR specific port - do NOT touch other ports!
          if lsof -i:${CYPRESS_PORT} > /dev/null 2>&1; then
            echo "⚠️  Port ${CYPRESS_PORT} is in use, cleaning up..."
            lsof -i:${CYPRESS_PORT}
            lsof -ti:${CYPRESS_PORT} | xargs -r kill -9 2>/dev/null || true
          else
            echo "✅ Port ${CYPRESS_PORT} is already free"
          fi
          
          # Give processes time to fully terminate
          sleep 3
          
          # Verify port is actually free with retry logic
          echo "🔍 Verifying port ${CYPRESS_PORT} is free..."
          RETRY_COUNT=0
          while lsof -i:${CYPRESS_PORT} > /dev/null 2>&1; do
            RETRY_COUNT=$((RETRY_COUNT + 1))
            if [ $RETRY_COUNT -gt 10 ]; then
              echo "❌ Port ${CYPRESS_PORT} still in use after cleanup!"
              echo "Processes using port:"
              lsof -i:${CYPRESS_PORT}
              echo ""
              echo "This may indicate:"
              echo "  1. A previous run didn't clean up properly"
              echo "  2. Another process is using this port"
              echo "  3. Port is in TIME_WAIT state"
              exit 1
            fi
            echo "⏳ Port ${CYPRESS_PORT} still in use, retrying cleanup... (attempt $RETRY_COUNT/10)"
            lsof -ti:${CYPRESS_PORT} | xargs -r kill -9 2>/dev/null || true
            sleep 2
          done
          
          echo "✅ Port ${CYPRESS_PORT} is free and ready!"
          
          echo "🚀 Starting webpack dev server with cluster proxy on port ${CYPRESS_PORT}..."
          cd frontend && ODH_PORT=${CYPRESS_PORT} npm run start:dev:ext &
          
          SERVER_PID=$!
          echo "SERVER_PID=$SERVER_PID" >> $GITHUB_ENV
          echo "Started webpack dev server with PID: $SERVER_PID on port ${CYPRESS_PORT}"
          
          # Give the server extra time to start in CI
          echo "⏳ Giving server 20 seconds to initialize..."
          sleep 20

      - name: Wait for Server Ready
        run: |
          echo "⏳ Waiting for localhost:${CYPRESS_PORT} to be ready..."
          # Use HTTP check instead of TCP since server is clearly running
          npx wait-on http://localhost:${CYPRESS_PORT} --timeout 120000
          echo "✅ Server is responding to HTTP!"
          
          # Verify the application actually loads with more details
          echo "🔍 Verifying application loads properly..."
          for i in {1..10}; do
            echo "🔄 Attempt $i/10: Testing application..."
            
            # Check if basic page loads
            if curl -s -f http://localhost:${CYPRESS_PORT}/ > /tmp/response.html 2>/dev/null; then
              echo "✅ HTTP 200 response received"
              
              # Check for dashboard content
              if grep -q "Data Science Projects\|ODH\|Open Data Hub\|Dashboard" /tmp/response.html; then
                echo "✅ Found dashboard content!"
                break
              else
                echo "⚠️  Page loads but no dashboard content yet..."
                echo "📄 Response contains: $(head -c 200 /tmp/response.html | tr '\n' ' ')"
              fi
            else
              echo "❌ HTTP request failed"
              curl_exit_code=$?
              echo "Curl exit code: $curl_exit_code"
            fi
            
            if [ $i -lt 10 ]; then
              echo "⏳ Waiting 8 seconds before retry..."
              sleep 8
            fi
          done
          
          # Final verification with detailed debugging
          echo "🔍 Final application verification..."
          if curl -s -f http://localhost:${CYPRESS_PORT}/ | grep -q "Data Science Projects\|ODH\|Open Data Hub\|Dashboard"; then
            echo "✅ Server is ready and application is loaded!"
            
            # Test API endpoint
            echo "🔍 Testing API endpoint..."
            if curl -s -f http://localhost:${CYPRESS_PORT}/api/status > /dev/null; then
              echo "✅ API proxy is working!"
            else
              echo "⚠️  API endpoint test failed, but frontend is working"
            fi
          else
            echo "❌ Application failed to load properly"
            echo ""
            echo "📊 Debugging information:"
            echo "🔄 Server process status:"
            ps aux | grep -E "(webpack|node.*${CYPRESS_PORT})" | head -5 || echo "No matching processes found"
            
            echo ""
            echo "🌐 Network connectivity test:"
            curl -I http://localhost:${CYPRESS_PORT}/ 2>&1 || echo "Connection failed"
            
            echo ""
            echo "📄 Raw response (first 1000 chars):"
            curl -s http://localhost:${CYPRESS_PORT}/ 2>/dev/null | head -c 1000 || echo "Failed to get response"
            
            echo ""
            echo "🔍 Looking for webpack output in background..."
            # Check if there are any relevant log files or output
            ls -la /tmp/ | grep -E "(webpack|error|log)" || echo "No relevant temp files found"
            
            exit 1
          fi

      - name: Debug Port Connectivity Before Cypress
        run: |
          echo "🔍 PRE-CYPRESS PORT DEBUGGING FOR: ${CYPRESS_PORT}"
          echo "=========================================="
          
          echo ""
          echo "1️⃣ System view - what's listening on port ${CYPRESS_PORT}:"
          lsof -i:${CYPRESS_PORT} || echo "✅ No processes shown by lsof"
          
          echo ""
          echo "2️⃣ Network connectivity test:"
          if curl -s -f http://127.0.0.1:${CYPRESS_PORT}/ > /dev/null; then
            echo "✅ IPv4 127.0.0.1 connection: SUCCESS"
          else
            echo "❌ IPv4 127.0.0.1 connection: FAILED"
          fi
          
          if curl -s -f http://localhost:${CYPRESS_PORT}/ > /dev/null; then
            echo "✅ localhost connection: SUCCESS"
          else
            echo "❌ localhost connection: FAILED"
          fi
          
          echo ""
          echo "3️⃣ Process tree for webpack:"
          ps aux | grep -E "(webpack|node.*${CYPRESS_PORT})" | grep -v grep || echo "No matching processes"
          
          echo ""
          echo "4️⃣ Test simple HTTP request that Cypress would make:"
          curl -I http://localhost:${CYPRESS_PORT}/ 2>&1 | head -10
          
          echo ""
          echo "=========================================="

      - name: Run E2E Tests
        run: |
          cd frontend

          echo "🧪 Running commit-based E2E tests..."
          echo "Event: ${{ github.event_name }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"

          # Run real E2E tests (not mock mode)
          echo "🚀 Running real E2E tests against live dashboard"

          # Run tests with matrix tag using Jenkins-style command format
          export CY_TEST_CONFIG="${{ github.workspace }}/frontend/src/__tests__/cypress/test-variables.yml"
          export CY_RESULTS_DIR="${{ github.workspace }}/frontend/src/__tests__/cypress/results/${{ matrix.test-tag }}"
          export BASE_URL="http://localhost:${CYPRESS_PORT}"
          
          # Also set CYPRESS_BASE_URL to ensure Cypress reads it
          export CYPRESS_BASE_URL="http://localhost:${CYPRESS_PORT}"
          
          echo "📍 Environment check before Cypress:"
          echo "   BASE_URL=$BASE_URL"
          echo "   CYPRESS_BASE_URL=$CYPRESS_BASE_URL"
          echo "   CYPRESS_PORT=${CYPRESS_PORT}"
          echo "   CY_TEST_CONFIG=$CY_TEST_CONFIG"

          # Ensure OpenShift configuration is available to Cypress process
          if [ -n "$KUBECONFIG" ]; then
            echo "🔧 OpenShift configuration available: $KUBECONFIG"
            # Verify oc command works in this context
            oc whoami || echo "⚠️  oc whoami failed, but continuing with tests"
          else
            echo "⚠️  No KUBECONFIG set, OpenShift commands may fail"
          fi

          echo "🎯 Running tests with tags: ${{ matrix.test-tag }}"
          echo "🚫 Skipping tags: @Bug,@Maintain,@NonConcurrent"
          echo "📋 Test group: ${{ matrix.test-tag }}"

          # Use Electron browser (more stable in CI than Chrome)
          echo "🔄 Using Electron browser"

          # Create results directory
          mkdir -p "$CY_RESULTS_DIR"

          # Run tests with baseUrl explicitly set on command line (highest precedence)
          # Add DEBUG env vars to get more Cypress verbosity on port/browser issues
          echo "🐛 Running with enhanced debugging for port issues..."
          DEBUG=cypress:server:browsers:electron,cypress:server:browsers,cypress:cli npm run cypress:run:electron -- \
            --env skipTags="@Bug @Maintain @NonConcurrent",grepTags="${{ matrix.test-tag }}",grepFilterSpecs=true \
            --config baseUrl=http://localhost:${CYPRESS_PORT},video=true,screenshotsFolder="$CY_RESULTS_DIR/screenshots",videosFolder="$CY_RESULTS_DIR/videos" 2>&1 | tee /tmp/cypress_run.log || {
            
            CYPRESS_EXIT_CODE=$?
            echo ""
            echo "❌ CYPRESS FAILED WITH EXIT CODE: $CYPRESS_EXIT_CODE"
            echo "=========================================="
            
            if grep -q "Port.*is already in use" /tmp/cypress_run.log; then
              echo "🔍 DETECTED 'Port ${CYPRESS_PORT} is already in use' ERROR!"
              echo ""
              echo "📊 Port status IMMEDIATELY AFTER failure:"
              lsof -i:${CYPRESS_PORT} || echo "✅ No processes on port ${CYPRESS_PORT} (lsof says free)"
              
              echo ""
              echo "🔍 Checking if Cypress/Electron tried to bind the port:"
              ps aux | grep -E "(cypress|electron)" | grep -v grep || echo "No cypress/electron processes"
              
              echo ""
              echo "🔍 Network statistics for port ${CYPRESS_PORT}:"
              netstat -an | grep ${CYPRESS_PORT} || echo "No netstat entries for port ${CYPRESS_PORT}"
              
              echo ""
              echo "📄 Last 60 lines of Cypress output:"
              tail -60 /tmp/cypress_run.log
              
              echo ""
              echo "💡 HYPOTHESIS: Cypress reports port in use, but system shows it's free."
              echo "   This suggests Cypress internal port checking issue, not actual conflict."
            else
              echo "📄 Last 50 lines of Cypress output:"
              tail -50 /tmp/cypress_run.log
            fi
            
            exit $CYPRESS_EXIT_CODE
          }

      - name: Test tag name
        if: ${{ always() }}
        run: |
          TEST_TAG_NAME=$(echo '${{ matrix.test-tag }}' | tr '/' '_' | tr '@' '_')
          echo "TEST_TAG_NAME=$TEST_TAG_NAME" >> $GITHUB_ENV

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-test-results-${{ matrix.test-tag }}
          path: |
            frontend/src/__tests__/cypress/results/
            frontend/src/__tests__/cypress/videos/
            frontend/src/__tests__/cypress/screenshots/
            frontend/src/__tests__/cypress/coverage/
          retention-days: 7

      - name: Log test completion
        if: always()
        run: |
          echo "🏁 E2E Test completed!"
          echo "Status: ${{ job.status }}"
          echo "Event: ${{ github.event_name }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "Test Tag: ${{ matrix.test-tag }}"
          echo "Commit: ${{ github.sha }}"
          echo "Run ID: ${{ github.run_id }}"
          echo ""
          echo "📊 Test artifacts uploaded to:"
          echo "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"

  cleanup-server:
    needs: [e2e-tests]
    runs-on: self-hosted
    if: ${{ always() && needs.e2e-tests.result != 'skipped' }}
    steps:
      - name: Stop Cypress Servers
        run: |
          echo "🛑 Stopping webpack dev server..."
          if [ -n "$SERVER_PID" ]; then
            kill $SERVER_PID || echo "Failed to kill SERVER_PID"
          fi
          pkill -f "webpack.*serve" || echo "No webpack processes found"
          pkill -f "node.*40[0-9][0-9]" || echo "No node processes on dynamic ports"
          echo "✅ Server cleanup completed"
