name: Test / AutoRAG BFF Tests
on:
  push:
    paths:
      - "packages/autorag/**"
      - "!LICENSE*"
      - "!DOCKERFILE*"
      - "!**.gitignore"
      - "!**.md"

  pull_request:
    paths:
      - "packages/autorag/**"
      - ".github/workflows/autorag-bff-tests.yml"
      - "!LICENSE*"
      - "!DOCKERFILE*"
      - "!**.gitignore"
      - "!**.md"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.1

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.24.6"

      - name: Lint
        working-directory: packages/autorag/bff
        run: make lint

      - name: Build (BFF)
        working-directory: packages/autorag/bff
        run: make build

      - name: Test (BFF)
        working-directory: packages/autorag/bff
        run: make test

      - name: Check if there are uncommitted file changes
        working-directory: packages/autorag
        run: |
          clean=$(git status --porcelain)
          if [[ -z "$clean" ]]; then
              echo "Empty git status --porcelain: $clean"
          else
              echo "Uncommitted file changes detected: $clean"
              git diff
              exit 1
          fi
