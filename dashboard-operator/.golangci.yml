version: "2"

linters:
  default: standard
  disable:
    - depguard
  settings:
    govet:
      enable-all: true
      disable:
        - fieldalignment
        - shadow

formatters:
  enable:
    - gofmt
    - goimports

issues:
  exclude-dirs:
    - bin
    - config
