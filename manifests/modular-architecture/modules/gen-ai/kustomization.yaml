apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - service-account.yaml
  - cluster-role.yaml
  - cluster-role-binding.yaml
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
configMapGenerator:
  - name: gen-ai-params
    env: params.env
generatorOptions:
  disableNameSuffixHash: true
vars:
  - name: gen-ai-ui-image
    objref:
      kind: ConfigMap
      name: gen-ai-params
      apiVersion: v1
    fieldref:
      fieldpath: data.gen-ai-ui-image
  - name: pgvector-image
    objref:
      kind: ConfigMap
      name: gen-ai-params
      apiVersion: v1
    fieldref:
      fieldpath: data.pgvector-image
configurations:
  - params.yaml
