apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# MaaS Consumer Portal ConsoleLink. Deployed conditionally by the
# dashboard-operator when spec.maasConsumerPortal.managementState is Managed and
# Gateway.Domain is set. The operator writes maas-consumer-portal-url and
# section-title into params.env before rendering.
#
# No commonLabels here: the portal is a separate product from rhods-dashboard,
# and the operator's deployer stamps platform.opendatahub.io/part-of=maas-consumer-portal.
resources:
  - consolelink.yaml
configMapGenerator:
  - name: maas-consumer-portal-params
    env: params.env
generatorOptions:
  disableNameSuffixHash: true
replacements:
  - source:
      kind: ConfigMap
      name: maas-consumer-portal-params
      fieldPath: data.section-title
    targets:
      - select:
          kind: ConsoleLink
          name: maas-consumer-portal-link
        fieldPaths:
          - spec.applicationMenu.section
  - source:
      kind: ConfigMap
      name: maas-consumer-portal-params
      fieldPath: data.maas-consumer-portal-url
    targets:
      - select:
          kind: ConsoleLink
          name: maas-consumer-portal-link
        fieldPaths:
          - spec.href
