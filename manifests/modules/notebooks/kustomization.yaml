apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - service-account.yaml
  - cluster-role.yaml
  - cluster-role-binding.yaml
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
configMapGenerator:
  - name: notebooks-params
    env: params.env
generatorOptions:
  disableNameSuffixHash: true
replacements:
  - source:
      kind: ConfigMap
      name: notebooks-params
      fieldPath: data.notebooks-ui-image
    targets:
      - select:
          kind: Deployment
          name: notebooks-ui
        fieldPaths:
          - spec.template.spec.containers.[name=notebooks-ui].image
