apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - service-account.yaml
  - cluster-role.yaml
  - cluster-role-binding.yaml
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
configMapGenerator:
  - name: mlflow-params
    env: params.env
generatorOptions:
  disableNameSuffixHash: true
replacements:
  - source:
      kind: ConfigMap
      name: mlflow-params
      fieldPath: data.mlflow-ui-image
    targets:
      - select:
          kind: Deployment
          name: mlflow-ui
        fieldPaths:
          - spec.template.spec.containers.[name=mlflow-ui].image
