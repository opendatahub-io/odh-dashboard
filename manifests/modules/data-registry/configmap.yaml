apiVersion: v1
kind: ConfigMap
metadata:
  name: data-registry-config
data:
  # TODO: If a future release allows users to create the Data Registry namespace,
  # derive this Service URL dynamically instead of keeping it hard-coded here.
  apiURL: https://feast-data-registry.redhat-ods-applications.svc:8443
