apiVersion: v1
kind: Secret
metadata:
  name: "{{RESOURCE_NAME}}"
  namespace: "{{PROJECT_NAME}}"
  labels:
    inference.llm-d.ai/ipp-managed: "true"
type: Opaque
stringData:
  api-key: llm-katan-openai-key
