apiVersion: v1
kind: Secret
metadata:
  name: "{{RESOURCE_NAME}}"
  namespace: "{{PROJECT_NAME}}"
  labels:
    inference.networking.k8s.io/bbr-managed: "true"
type: Opaque
stringData:
  api-key: llm-katan-openai-key
