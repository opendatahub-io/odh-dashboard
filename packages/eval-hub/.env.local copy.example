# Copy this file to `.env.local` (not committed) and modify the values as needed.

# EvalHub API URL - point to your local or remote eval-hub instance
# For local development with port-forwarding:
#   oc port-forward -n <namespace> svc/evalhub 8080:8080
EVAL_HUB_URL=http://localhost:8080

# Namespace where the EvalHub service is deployed
EVAL_HUB_NAMESPACE=evalhub-test

# EvalHub Kubernetes service name and ports (used by dev-portforward)
EVAL_HUB_SVC_NAME=evalhub
EVAL_HUB_LOCAL_PORT=8080
EVAL_HUB_SVC_PORT=8080
