openapi: 3.0.3
info:
  title: MaaS BFF API
  version: 1.0.0
  description: Backend for Frontend (BFF) API for Model as a Service (MaaS)
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html

servers:
  - url: /maas
    description: Base path for MaaS BFF

security:
  - bearerAuth: []

paths:
  /healthcheck:
    get:
      summary: Health Check
      operationId: getHealthCheck
      description: Returns the health status of the service
      security: []
      responses:
        '200':
          description: Service is healthy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
              example:
                status: healthy
        '500':
          description: Internal Server Error

  /api/v1/user:
    get:
      summary: Get Current User
      operationId: getCurrentUser
      description: Returns information about the currently authenticated user
      responses:
        '200':
          description: User information
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/User'
        '400':
          description: Bad Request (missing identity)
        '401':
          description: Unauthorized
        '500':
          description: Internal Server Error

  /api/v1/namespaces:
    get:
      summary: Get Namespaces
      operationId: getNamespaces
      description: Returns a list of namespaces accessible to the user
      responses:
        '200':
          description: List of namespaces
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Namespace'
        '400':
          description: Bad Request
        '401':
          description: Unauthorized
        '500':
          description: Internal Server Error

  /api/v1/models:
    get:
      tags:
        - models
      summary: List MaaS Models
      operationId: listModels
      description: Lists available large language models in OpenAI-compatible format
      responses:
        '200':
          description: List of models
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ModelListResponse'
              example:
                object: list
                data:
                  - created: 1672531200
                    id: llama-2-7b-chat
                    object: model
                    owned_by: model-namespace
                    ready: true
                    url: https://api.example.com/v1/models/llama-2-7b-chat
                  - created: 1672531200
                    id: mistral-7b-instruct
                    object: model
                    owned_by: model-namespace
                    ready: true
                    url: https://api.example.com/v1/models/mistral-7b-instruct
                  - created: 1672531200
                    id: granite-8b-code-instruct
                    object: model
                    owned_by: model-namespace
                    ready: false
                  - created: 1672531200
                    id: llama-3-8b-instruct
                    object: model
                    owned_by: model-namespace
                    ready: true
                    url: https://api.example.com/v1/models/llama-3-8b-instruct
        '401':
          description: Unauthorized
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                error: Failed to retrieve LLM models

  /api/v1/api-keys:
    post:
      tags:
        - api-keys
      summary: Create a new hash-based API key
      operationId: createApiKey
      description: Creates a new OpenAI-compatible API key (sk-oai-* format). Supports both permanent keys (no expiration) and expiring keys (with expiresIn parameter). The plaintext key is shown ONLY ONCE at creation time and cannot be retrieved again.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                  description: Human-readable name for the API key
                description:
                  type: string
                  description: Optional description
                expiresIn:
                  type: string
                  description: Optional expiration duration (e.g., "30d", "90d", "1h"). Omit for permanent key.
            examples:
              permanent_key:
                summary: Permanent API key (no expiration)
                value:
                  name: my-permanent-key
                  description: Production API key
              expiring_key:
                summary: Expiring API key (90 days)
                value:
                  name: my-expiring-key
                  description: 90-day test key
                  expiresIn: 90d
      responses:
        '201':
          description: Created response.
          content:
            application/json:
              schema:
                type: object
                properties:
                  key:
                    type: string
                    description: Plaintext API key (SHOWN ONCE, NEVER AGAIN)
                  keyPrefix:
                    type: string
                    description: Display prefix for UI (e.g., sk-oai-abc)
                  id:
                    type: string
                    description: Unique identifier for the key
                  name:
                    type: string
                    description: Name of the key
                  createdAt:
                    type: string
                    format: date-time
                    description: Creation timestamp (RFC3339)
                  expiresAt:
                    type: string
                    format: date-time
                    description: Expiration timestamp (RFC3339), omitted for permanent keys
        '400':
          description: Bad Request response.
        '401':
          description: Unauthorized response.
        '500':
          description: Internal Server Error

  /api/v1/api-keys/search:
    post:
      tags:
        - api-keys
      summary: Search and filter API keys
      operationId: searchApiKeys
      description: Search API keys with flexible filtering, sorting, and pagination. Supports filtering by username (admin-only), status, sorting by multiple fields, and pagination.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  properties:
                    username:
                      type: string
                      description: Filter by username (admin-only)
                    status:
                      type: array
                      items:
                        type: string
                        enum: [active, revoked, expired]
                      description: Filter by status (active, revoked, expired). Defaults to active only.
                sort:
                  type: object
                  properties:
                    by:
                      type: string
                      enum: [created_at, expires_at, last_used_at, name]
                      description: Field to sort by
                    order:
                      type: string
                      enum: [asc, desc]
                      description: Sort order
                pagination:
                  type: object
                  properties:
                    limit:
                      type: integer
                      minimum: 1
                      maximum: 100
                      default: 50
                    offset:
                      type: integer
                      minimum: 0
                      default: 0
            examples:
              default_search:
                summary: Default search (active keys, newest first)
                value:
                  filters:
                    status: ["active"]
                  sort:
                    by: "created_at"
                    order: "desc"
                  pagination:
                    limit: 50
                    offset: 0
              admin_search:
                summary: Admin searching user's keys
                value:
                  filters:
                    username: "alice"
                    status: ["active", "revoked"]
                  sort:
                    by: "name"
                    order: "asc"
                  pagination:
                    limit: 20
                    offset: 0
      responses:
        '200':
          description: OK response.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiKeyListResponse'
              example:
                object: list
                data:
                  - id: key_abc123
                    name: my-production-key
                    creationDate: "2024-01-15T10:30:00Z"
                    status: active
                  - id: key_def456
                    name: my-test-key
                    creationDate: "2024-01-14T09:15:00Z"
                    expirationDate: "2024-04-14T09:15:00Z"
                    status: active
                has_more: true
        '400':
          description: Bad Request. Invalid search parameters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized response.
        '500':
          description: Internal Server Error

  /api/v1/api-keys/bulk-revoke:
    post:
      tags:
        - api-keys
      summary: Bulk revoke API keys for a user
      operationId: bulkRevokeApiKeys
      description: Revokes all active API keys for a specific user. Regular users can only bulk revoke their own keys. Admins can bulk revoke any user's keys.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
              properties:
                username:
                  type: string
                  description: Username whose keys should be revoked
            examples:
              revoke_own:
                summary: User revoking own keys
                value:
                  username: "alice"
              admin_revoke:
                summary: Admin revoking user's keys
                value:
                  username: "bob"
      responses:
        '200':
          description: OK response.
          content:
            application/json:
              schema:
                type: object
                properties:
                  revokedCount:
                    type: integer
                    description: Number of keys that were revoked
                  message:
                    type: string
                    description: Success message
              example:
                revokedCount: 5
                message: "Successfully revoked 5 active API key(s) for user alice"
        '400':
          description: Bad Request. Invalid request.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized response.
        '403':
          description: Forbidden. User trying to revoke another user's keys.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                error: "Access denied: you can only bulk revoke your own API keys"
        '500':
          description: Internal Server Error

  /api/v1/api-keys/{id}:
    get:
      tags:
        - api-keys
      summary: Get a specific API key by ID
      operationId: getApiKey
      description: Returns metadata for a single API key by its ID.
      parameters:
        - in: path
          name: id
          schema:
            type: string
          required: true
          description: ID of the API key to retrieve
      responses:
        '200':
          description: OK response.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiKey'
        '401':
          description: Unauthorized response.
        '404':
          description: Not Found. API key not found.
        '500':
          description: Internal Server Error
    delete:
      tags:
        - api-keys
      summary: Revoke a specific API key
      operationId: revokeApiKey
      description: Revokes a specific API key by changing its status to 'revoked'. The key will no longer be valid for authentication but remains visible in API key lists for audit purposes.
      parameters:
        - in: path
          name: id
          schema:
            type: string
          required: true
          description: ID of the API key to revoke
      responses:
        '200':
          description: OK. Key successfully revoked.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiKey'
              example:
                id: key_abc123
                name: my-production-key
                creationDate: "2024-01-15T10:30:00Z"
                status: revoked
        '401':
          description: Unauthorized response.
        '403':
          description: Forbidden. User trying to revoke another user's key.
        '404':
          description: Not Found. API key not found.
        '500':
          description: Internal Server Error

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
          description: Error message
          example: Failed to retrieve models
      required:
        - error

    HealthResponse:
      type: object
      properties:
        status:
          type: string
          description: Health status
          example: healthy
      required:
        - status

    User:
      type: object
      properties:
        userId:
          type: string
        clusterAdmin:
          type: boolean

    Namespace:
      type: object
      properties:
        name:
          type: string
        displayName:
          type: string

    ModelListResponse:
      type: object
      properties:
        object:
          type: string
          description: Object type, always "list"
          example: list
        data:
          type: array
          items:
            $ref: '#/components/schemas/Model'
          description: Array of model objects
      required:
        - object
        - data

    Model:
      type: object
      properties:
        id:
          type: string
          description: The model identifier
          example: llama-2-7b-chat
        object:
          type: string
          description: The object type, always "model"
          example: model
        created:
          type: integer
          description: The Unix timestamp (in seconds) when the model was created
          example: 1672531200
          format: int64
        owned_by:
          type: string
          description: The organization that owns the model
          example: model-namespace
        ready:
          type: boolean
          description: Model ready status
          example: true
        url:
          type: string
          description: Model URL (optional)
          example: https://api.example.com/v1/models/llama-2-7b-chat
      example:
        created: 1672531200
        id: llama-2-7b-chat
        object: model
        owned_by: model-namespace
        ready: true
        url: https://api.example.com/v1/models/llama-2-7b-chat
      required:
        - id
        - object
        - created
        - owned_by
        - ready

    ApiKey:
      type: object
      properties:
        id:
          type: string
          description: Unique identifier for the API key
        name:
          type: string
          description: User-friendly name for the API key
        description:
          type: string
          description: Optional description providing context about the key's purpose
        username:
          type: string
          description: Username that owns this API key
        groups:
          type: array
          items:
            type: string
          description: User's groups at creation time (immutable snapshot for authorization)
        creationDate:
          type: string
          format: date-time
          description: When the API key was created
        expirationDate:
          type: string
          format: date-time
          description: When the API key expires (empty for permanent keys)
        status:
          type: string
          enum: [active, revoked, expired]
          description: Current status of the API key
        lastUsedAt:
          type: string
          format: date-time
          description: When the API key was last used for validation
      required:
        - id
        - name
        - creationDate
        - status

    ApiKeyListResponse:
      type: object
      properties:
        object:
          type: string
          description: Object type, always "list"
          example: list
        data:
          type: array
          items:
            $ref: '#/components/schemas/ApiKey'
          description: Array of API key metadata objects
        has_more:
          type: boolean
          description: Whether there are more results beyond this page
          example: true
      required:
        - object
        - data
        - has_more
