# MLflow UI Architecture

## Overview

![Overview](./meta/arch-overview.png)

The MLflow UI is a web application that provides a user interface for interacting with the MLflow API. The UI is built using React and communicates with the MLflow API using RESTful HTTP requests. The UI is designed to be responsive and accessible, providing a user-friendly experience for users interacting with MLflow.
