{
  "private": true,
  "name": "@odh-dashboard/mlflow",
  "description": "MLflow plugin.",
  "version": "0.0.0",
  "module-federation": {
    "name": "mlflow",
    "remoteEntry": "/remoteEntry.js",
    "authorize": true,
    "tls": false,
    "proxy": [
      {
        "path": "/_bff/mlflow/api",
        "pathRewrite": "/api"
      }
    ],
    "local": {
      "host": "localhost",
      "port": 9110
    },
    "service": {
      "name": "odh-dashboard",
      "port": 8043
    }
  },
  "e2eCiTags": ["@MLflowCI"],
  "bffConfig": {
    "enabled": true,
    "port": 4020,
    "healthEndpoint": "/healthcheck",
    "startCommand": "make dev-bff-e2e-mock",
    "startCommandCluster": "make dev-bff-e2e-cluster"
  },
  "scripts": {
    "install:module": "npm install --prefix frontend",
    "test:contract": "cross-env BFF_MOCK_FLAGS='--mock-k8s-client --mock-http-client --auth-method=disabled' odh-ct-bff-consumer --bff-dir bff",
    "cypress:server:build": "cd frontend && DIST_DIR=./public-cypress DEPLOYMENT_MODE=federated npm run build",
    "cypress:server:build:coverage": "COVERAGE=true npm run cypress:server:build",
    "cypress:server": "cd frontend && serve ./public-cypress -p 9110 -s -L",
    "cypress:server:dev": "cd frontend && DEPLOYMENT_MODE=federated PORT=9110 PROXY_PORT=4020 npm run start:dev",
    "cypress:server:wait": "npx wait-on -i 1000 http://localhost:9110",
    "lint": "cd frontend && npm run lint",
    "lint:fix": "cd frontend && npm run lint:fix"
  },
  "dependencies": {
    "@odh-dashboard/internal": "*",
    "@odh-dashboard/plugin-core": "*"
  },
  "devDependencies": {
    "@odh-dashboard/contract-tests": "*",
    "@odh-dashboard/eslint-config": "*",
    "@odh-dashboard/tsconfig": "*",
    "cross-env": "^10.1.0",
    "eslint-plugin-n": "^17.23.1"
  }
}
