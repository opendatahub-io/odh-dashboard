# Python dependencies for the Llama Stack test server.
# Prevent upgrading to versions that could break the test server.
# Versions may be updated when the Llama Stack test server is upgraded for compatibility.
aiohttp>=3.9
milvus-lite==2.5.1
pymilvus==2.6.9
chardet==7.1.0
ollama==0.6.1
pypdf==6.9.0
regex>=2024.11.6
rich>=13
# tiktoken imports requests at runtime; reject TestPyPI's Python 3.12-incompatible release.
requests>=2.32,<3
setuptools>=75,<81
