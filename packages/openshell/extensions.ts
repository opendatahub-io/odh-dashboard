// UI extensions are loaded at runtime via Module Federation (`openshell/extensions`).
// Keep this export empty to avoid duplicate registration with the static host bundle.
const extensions: unknown[] = [];

export default extensions;
