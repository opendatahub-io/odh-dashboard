/**
 * Shared AutoX `feature` components.
 *
 * Combine primitives with product-agnostic business logic and hooks. Each
 * fulfills exactly one business need. Accept a strategy object and/or named
 * slot props as the sole extension points for product-specific customization —
 * never fully-resolved product data as a prop override.
 *
 * See ../../../ARCHITECTURE.md for the full layering conventions.
 */
