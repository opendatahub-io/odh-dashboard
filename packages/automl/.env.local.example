# This file serves as an example environment configuration for local development.
# Copy this file to `.env.local` (not committed) and modify the values as needed.

# Application Environment
APP_ENV=local

# Deployment mode: standalone, kubeflow, or federated
# - standalone: Run AutoML as independent application (Material UI theme)
# - kubeflow: Integrate with Kubeflow Dashboard (Material UI theme)
# - federated: Load as micro-frontend in ODH/RHOAI Dashboard (PatternFly theme)
DEPLOYMENT_MODE=standalone

# Development Mode (enables extra logging and dev features)
DEV_MODE=true

# Logging Level: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO
