{
  "private": true,
  "name": "@odh-dashboard/automl",
  "description": "AutoML plugin.",
  "version": "0.0.0",
  "module-federation": {
    "name": "automl",
    "remoteEntry": "/remoteEntry.js",
    "authorize": true,
    "tls": false,
    "proxy": [
      {
        "path": "/automl/api",
        "pathRewrite": "/api"
      }
    ],
    "local": {
      "host": "localhost",
      "port": 9104
    },
    "service": {
      "name": "odh-dashboard",
      "port": 8043
    }
  },
  "scripts": {
    "install:module": "npm install --prefix frontend",
    "cypress:server:build": "cd frontend && DIST_DIR=./public-cypress DEPLOYMENT_MODE=federated npm run build",
    "cypress:server:build:coverage": "COVERAGE=true npm run cypress:server:build",
    "cypress:server": "cd frontend && serve ./public-cypress -p 9104 -s -L",
    "cypress:server:dev": "cd frontend && DEPLOYMENT_MODE=federated PORT=9104 npm run start:dev",
    "cypress:server:wait": "npx wait-on -i 1000 http://localhost:9104"
  },
  "dependencies": {
    "@odh-dashboard/internal": "*",
    "@odh-dashboard/plugin-core": "*"
  },
  "devDependencies": {
    "@odh-dashboard/eslint-config": "*",
    "@odh-dashboard/tsconfig": "*",
    "eslint-plugin-n": "^17.23.1"
  }
}
