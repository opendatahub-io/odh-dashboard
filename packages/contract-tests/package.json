{
  "name": "@odh-dashboard/contract-tests",
  "version": "0.1.0",
  "description": "Centralized contract testing utilities for ODH Dashboard",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "odh-contract-test": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.standalone.json",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src --ext .ts,.tsx",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@jest/reporters": "^29.7.0",
    "jest": "^29.7.0",
    "jest-html-reporter": "^3.1.5",
    "ts-jest": "^29.1.1",
    "typescript": "^5.0.0",
    "axios": "^1.6.0",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0"
  },
  "peerDependencies": {
    "jest": "^29.0.0"
  }
}
